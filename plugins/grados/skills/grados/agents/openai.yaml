interface:
  display_name: "GRaDOS"
  short_description: "Academic paper search, full-text extraction, and evidence-grounded writing"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "grados"
      description: "GRaDOS MCP server for academic paper search, full-text extraction, and evidence-grounded writing"
      transport: "stdio"
